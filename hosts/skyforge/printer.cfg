# Stub config — paste real Voron Trident printer.cfg here later from
# ~/trident-backup/printer_data/config/printer.cfg (workstation backup).
# When pasting: rewrite /home/voron/printer_data → /var/lib/klipper and
# drop update_manager + add-on includes.

[mcu]
serial: /dev/null

[printer]
kinematics: none
max_velocity: 100
max_accel: 100

[virtual_sdcard]
path: /var/lib/klipper/gcodes
